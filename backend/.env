# .env 文件内容（已匹配账号px/123456）
DB_SERVER=localhost  # 你的SQL Server实例名（之前用的，不用改）
DB_USER=px                      # 登录名（px账号，不用改）
DB_PASSWORD=123456       # 替换为你设置的px密码（如Px123456，必须填对！）
DB_NAME=peixun                # 目标数据库名（你创建的“培训机构”，不用改）
DB_PORT=1433                    # SQL Server默认端口（不用改）
API_PORT=3003 # 后端服务端口